<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.meer.model.dao.WordDao">


	<select id="selectFortune" resultType="String" parameterType="String">
		select fortune_word from fortune, user
		WHERE user.user_id = #{userId} 
		and user.fortune_number = fortune.fortune_id;
	</select>

	<select id="selectSentence" parameterType="String" resultType="String">
		select sentence_word, sentence_author from sentence, user
		WHERE user.user_id = #{userId} 
		and user.sentence_number = sentence.sentence_id;
	</select>
</mapper>